#include <limits.h>
#include <float.h>
#include <stdio.h>

int main() {
  
  printf("INT_MAX is %d\n", INT_MAX);
  printf("INT_MIN is %d\n", INT_MIN);
  printf("LONG_MAX is %ld\n", LONG_MAX);
  printf("LONG_MIN is %ld\n", LONG_MIN);
  printf("DBL_MAX is %le\n", DBL_MAX);
  printf("DBL_MIN is %le\n", DBL_MIN);
  
}